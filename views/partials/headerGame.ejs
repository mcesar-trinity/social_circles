<!DOCTYPE html>
<html> 
    <header class="heading"> 
        <h1><%= title %></h1>
        <div class="header-content">
            <nav class="main-nav">
                <ul>
                    <li><a href="/">Home</a></li>
                    <li><a href="/leaderboard">Leaderboard</a></li>
                    <li><a href="/game">Play the Game</a></li>
                    <li><a href="/char_dropdown">Character Page</a></li>
                </ul>
            </nav>
        </div>
        <% if (isUser) { %>
            <form action="/dashboard">
                <button 
                    id="profileCircle"
                    type="submit" 
                    class="account-button" 
                    style="background-color: <%= user.profile_color || '#808080' %>; transition: background-color 0.5s, color 0.5s;">
                    <span id="profileUsername" style="color:#ffffff ;transition: color 0.5s;">
                        <%= user?.username || 'Profile' %>
                    </span>
                </button>
            </form>
        <% } %>
    </header>

    <head>
        <title><%= typeof webTitle !== 'undefined' ? webTitle : 'Social Circles' %></title>
        <link rel='stylesheet' type='text/css' href='/stylesheets/game.css' />
	</head>